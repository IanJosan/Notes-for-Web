遇到重复标签可以使用封装组件，本质是独立的vue文件放在components中，在App.vue中import引用。
引用完成后在export default中注册
components:{
    "组件命名"：“引入名”
}
优点：组件各自独立，便于复用
-----------------------------------------
组件是可复用的vue实例，封装标签，样式和JS代码。
组件化：封装思想，把页面上可重用部分封装为组件，从而方便项目的开发和维护。

组件封装开发的目的：复用，拆解