返回数组索引号的方法
arr.indexOf('数组元素'，[返回开始的索引号]);
只返回第一个满足条件的索引号 默认从0号开始
没有满足条件的就返回-1
arr.lastIndexOf('数组元素');
从后开始查找 返回满足条件的索引号

根据索引返回字符
arr.charAt(索引号)；
arr.charCodeAt(索引号) 返回相对应字符ASCII的值 判断用户按下哪个键
arr[索引号] H5新增